#include <FreeImage.h>

int main()
{
    // Save screenshot to file.
//    FreeImage_Initialise();
//    FIBITMAP *image = FreeImage_ConvertFromRawBits(&pixels[0], width, height, width*3, 24, 0xFF0000, 0x00FF00, 0x0000FF, true);
//    std::cout << "Saving screenshot: " << outputFileName << std::endl;
//    FreeImage_Save(FIF_PNG, image, outputFileName.c_str(), 0);
//    FreeImage_DeInitialise();
    return 0;
}
